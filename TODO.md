# Add Filter to Admin Dashboard for Merchants

## Tasks
- [x] Add state variables for filter criteria (category, risk profile, name search)
- [x] Add filter UI section above the merchants list with dropdowns and text input
- [x] Implement filtering logic to filter the merchants array based on selected criteria
- [x] Ensure filter updates the displayed list dynamically
- [x] Test that newly created merchants are also filtered
- [x] Fix API calls to use database ID instead of merchant_id for PATCH operations
- [x] Fix single merchant fetch to use database ID from list response
- [x] Change QR code download to JPEG format instead of PDF
